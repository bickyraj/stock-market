apiVersion: v1
kind: Service
metadata:
  name: argocd-server
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-server
    app.kubernetes.io/part-of: argocd
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080 # Updated to target port 7070
  selector:
    app.kubernetes.io/name: argocd-server
  type: LoadBalancer           # or NodePort if you prefer